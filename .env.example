# Copy this file to `.env` (never commit real secrets).
# Used ONLY by scripts/import-exercises.ts (server-side import).

SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_replace_me